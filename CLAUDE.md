# ChatDS project continuation

Read `SESSION_HANDOFF.md` completely before working. It contains the current Git baseline, production deployment, session diagnoses, test evidence, security boundaries, and exact next steps.

Treat the older `_SESSION_STATUS.md`, `_HARNESS_CHANGES.md`, `_REMOTE_OPS.md`, and `harness/HARNESS_V23_CONTEXT_2026-07-16.md` as historical context only. Never copy credentials from chat or old notes into files or command output. Preserve the dirty worktree, keep Harness fixes generic across standard-format Skills, and do not automatically run the model-heavy V2.3 E2E unless the user requests it.
